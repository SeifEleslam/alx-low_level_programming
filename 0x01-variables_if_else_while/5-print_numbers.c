#include <stdio.h>
/**
 * main - Entry point
 * Description: 'the program prints a fixed text to the standard error'
 * Return: Always 0 (Success)
 */
int main(void)
{
char l;

for (l = 0; l <= 9; l++)
{
printf("%d", l);
}
printf("\n");
return (0);
}
