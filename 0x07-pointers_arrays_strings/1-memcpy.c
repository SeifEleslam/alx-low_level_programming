#include "main.h"

/**
 * _memcpy - reset the parsing value to 98
 * @dest: first passing pointer
 * @src: second passing pointer
 * @n: number of repeated times
 * Return: pointer to dest
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i;

	for (i = 0; i < n; i++)
	{
		dest[i] = src[i;
	}

	return (dest);
}

