#include "main.h"

/**
 * mul - check if c is a digit or not.
 * @a: first int to multiply
 * @b: second int to mutiply
 * Return: result of multiplication.
 */

int mul(int a, int b)
{
        return (a * b)
}

